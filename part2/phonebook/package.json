{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "watch": "nodemon index.js",
    "build:ui": "rm -fr build && cd ../phonebook-ui && npm run build --prod && mv build ../phonebook/",
    "deploy": "cd ../../ && git subtree push --prefix part2/phonebook heroku master",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",
    "logs:prod": "heroku logs --tail"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.7.3"
  },
  "devDependencies": {
    "dotenv": "^8.1.0",
    "morgan": "^1.9.1",
    "nodemon": "^1.19.3"
  }
}
